# mcp-rag
Agentic Hybrid RAG Using MCP Servers

## System Requirements

This project requires:

- Python 3.10+
- Node.js 18+  
- Node.js 20+ recommended for MCP filesystem server compatibility

## Install Python Dependencies

```bash
pip install -r requirements.txt
